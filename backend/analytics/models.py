# Analytics models - no database models needed, using aggregation queries
from django.db import models

# This app uses aggregation queries on existing models
# No additional database models required
